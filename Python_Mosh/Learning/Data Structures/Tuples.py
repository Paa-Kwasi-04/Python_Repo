# it is simply a read only list
# so like list we can concatenate ans multiple


# Ways to create tuples
# point = (1, 2)
# point = 1, 2  # same thing
# point = 1,
# point = ()  # empty tuple
# point = (1, 2) + (3, 4)
# point = (1, 2)*3
# point = tuple([1, 2])  # takes any iterable

# slicing
point = (1, 2, 3)
print(point[0:2])


# Unpacking
x, y, z = point
