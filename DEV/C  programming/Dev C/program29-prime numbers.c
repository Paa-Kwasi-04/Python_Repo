////TODO
// Created by User on 6/22/2023.
//Write a program to check a number whether it is prime number or not.
//Get the range of numbers in the number
// check is number is divisible by them
// if the sum of the numbers it is divisible by is two then it is prime

#include <stdio.h>
#include <math.h>


int main() {

    int n,i,j;

    // taking the input from the  user
    printf("Enter a number: "); scanf("%d",&n);
    int a[n];

    // Use a for loop anf if condition to check
    for(i = 0;i<n;i++){
        int y =1;
        a[i];
        y++;
    }

    for()



    return 0;
}
